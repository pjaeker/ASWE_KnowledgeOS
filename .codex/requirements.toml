# Active project-scoped requirements aligned to the V2 Evidence-Intake self-evolution startset.

allowed_approval_policies = ["never"]
allowed_sandbox_modes = ["workspace-write"]
allowed_web_search_modes = ["cached", "live"]
