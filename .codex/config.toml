# Active project-scoped config for Evidence-Intake self-evolution on the V2 startset.
#:schema https://developers.openai.com/codex/config-schema.json

approval_policy = "never"
sandbox_mode = "workspace-write"
web_search = "live"
project_doc_fallback_filenames = ["PROJECT_INSTRUCTIONS_ASWE_EvidenceIntake_SelfEvolution_20260414_V2.txt"]
project_root_markers = ["AGENTS.md", ".git", ".codex/config.toml"]

[sandbox_workspace_write]
network_access = true

[features]
unified_exec = true
