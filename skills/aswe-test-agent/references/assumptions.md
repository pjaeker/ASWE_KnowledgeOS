## Assumptions

This package is derived from the current repo-wide roles model. Repo-local `.codex` implementations may specialize it. For `aswe-triager`, repo-local `.codex` materialization is not yet confirmed.
